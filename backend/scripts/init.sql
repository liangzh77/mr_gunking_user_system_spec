-- PostgreSQL initialization script
-- This file is executed when the database is first created

-- Enable UUID extension (will be created by Alembic migration)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Placeholder: Alembic will handle all schema creation
SELECT 'Database initialized. Ready for Alembic migrations.' AS status;
