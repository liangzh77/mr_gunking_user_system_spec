# =============================================================================
# MR游戏运营管理系统 - 生产环境配置模板
# =============================================================================
# 使用说明:
# 1. 复制此文件为 .env.prod: cp .env.prod.example .env.prod
# 2. 修改所有密码和密钥为你自己的安全值
# 3. 不要将 .env.prod 提交到 Git 仓库

# 数据库配置
POSTGRES_USER=mr_admin_prod
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD_12345678
POSTGRES_DB=mr_game_ops_prod

# Redis配置
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD

# 应用安全配置
ENCRYPTION_KEY=CHANGE_THIS_32_BYTES_ENCRYPT_KEY
JWT_SECRET_KEY=CHANGE_THIS_JWT_SECRET_KEY_64_BYTES_OR_MORE_FOR_SECURITY
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=43200

# CORS配置 (生产环境请使用实际域名)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# API配置
API_V1_PREFIX=/api/v1
API_BASE_URL=https://yourdomain.com/api/v1

# 监控配置
GRAFANA_PASSWORD=CHANGE_THIS_GRAFANA_PASSWORD

# 应用配置
APP_NAME=MR游戏运营管理系统
APP_VERSION=1.0.0
ENVIRONMENT=production
DEBUG=false

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=json

# 性能配置
MAX_CONNECTIONS=100
WORKERS=4
TIMEOUT=120

# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png,xlsx
